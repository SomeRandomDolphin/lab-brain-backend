# Module 5 Month 6 — Multimodal Conversational Agent
# Install with: pip install -r requirements.txt

# ── Month 1 (unchanged) ───────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
websockets>=12.0
numpy>=1.26.0

# ── Month 2 (unchanged) ───────────────────────────────────────────────────────
openai>=1.30.0
pydantic>=2.0.0

# ── Month 3 (unchanged) ───────────────────────────────────────────────────────
python-dateutil>=2.9.0

# ── Month 4 (unchanged) ───────────────────────────────────────────────────────
whisperx>=3.1.5
pyannote.audio>=3.3.2
torch>=2.1.0
scikit-learn>=1.4.0

# ── Month 5 (unchanged) ───────────────────────────────────────────────────────
sentence-transformers==3.3.1
spacy>=3.7.0

# ── Month 6 additions ─────────────────────────────────────────────────────────

# LiveKit Python SDK — room management, token signing, server-side participant
livekit>=0.11.0
livekit-api>=0.6.0

# Audio resampling: converts LiveKit's 48kHz int16 stereo to 16kHz mono float32
# before feeding into the existing VadChunker / WhisperX pipeline.
soxr>=0.3.7

# PyAV is pulled by whisperx; listed explicitly for the video frame decoder
# used in the LiveKit video subscriber.
av>=12.0.0