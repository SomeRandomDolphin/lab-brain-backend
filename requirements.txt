# Module 5 Month 2 — Multimodal Conversational Agent
# Install with: pip install -r requirements.txt

# ── Month 1 (unchanged) ───────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
websockets>=12.0
faster-whisper>=1.0.0
numpy>=1.26.0

# ── Month 2 additions ─────────────────────────────────────────────────────────

# Gemini 2.0 Flash — VLM perception (vision.py) + dialogue agent (dialogue.py)
# Set GEMINI_API_KEY env var before running. Without it, vision runs in stub mode.
google-generativeai>=0.8.0

# LKC retrieval — TF-IDF index over lkc_stream.jsonl (lkc_retrieval.py)
scikit-learn>=1.4.0

# Pydantic v2 is already a FastAPI dependency; listed here for clarity
pydantic>=2.0.0

# ── Month 3 (not needed yet) ──────────────────────────────────────────────────
# pyannote.audio>=3.3   # real speaker diarization (replaces round-robin placeholder)
# whisperx              # word-level timestamps + diarization alignment
# sentence-transformers # dense embeddings for LKC retrieval upgrade